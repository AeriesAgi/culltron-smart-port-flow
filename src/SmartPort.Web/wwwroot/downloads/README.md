# Smart Port downloads

Place the Android debug APK here as `SmartPortDriverCompanion-debug.apk` before deployment if you want `/fleet/download-app` to expose a direct download button.

Build source path: `mobile/SmartPortDriverCompanion`.
CI artifact workflow: `.github/workflows/build-android-apk.yml`.
